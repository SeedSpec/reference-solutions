# College Football Dashboard — external-link comparison package

This package is the third arm of a controlled implementation comparison. It
contains the same product definition, configuration, capability contract, and
acceptance criteria as the plain and Andromeda-guided packages.

Its implementation profile names the public AI Canvas Andromeda design system
as the preferred realization and links to its public page. It includes no
Andromeda source, tokens, rules, component mapping, screenshots, or
Andromeda-specific implementation skill.

It retains the same optional Context7 instruction resource as the other two
packages.

```bash
npx @seedspec/cli validate solutions/college-football-dashboard-linked/seedspec
npx @seedspec/cli begin solutions/college-football-dashboard-linked/seedspec
```
